// Запросите у пользователя длину стороны квадрата и выведите площадь такого квадрата.

// alert("площадь " + Math.pow(prompt("сторона"), 2));

let side;

side = prompt("Введите сторону квадрата");

let area;

area = side * side;

alert("Площадь квадрата равна " + area);